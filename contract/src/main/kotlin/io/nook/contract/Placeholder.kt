package io.nook.contract

import kotlinx.serialization.Serializable

/** Placeholder DTO proving the serialization wiring; real contract types replace it. */
@Serializable
data class Placeholder(val placeholder: Boolean = true)
