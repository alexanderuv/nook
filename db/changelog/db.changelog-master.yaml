# Nook — Liquibase master changelog.
#
# The schema is plain standard SQL (no partial indexes or other DB-specific
# features). PostgreSQL is the sole supported engine — production, development,
# and tests (embedded binaries) all run it.
databaseChangeLog:
  - includeAll:
      path: changes
      relativeToChangelogFile: true
