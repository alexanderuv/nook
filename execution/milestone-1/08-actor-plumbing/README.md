# Epic 08 — Actor plumbing

**Addresses:** REQ8 (P1 — `created_by`/`updated_by` subject strings on every
mutation, server-populated read-only `owner_subject` on projects).

Placeholder — to be expanded in a later document.
