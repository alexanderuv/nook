# Epic 07 — Web API

**Addresses:** REQ7 (the catalog mirrored in RPC style at
`/api/{projectRef}/…` and `/api/projects`, shared DTOs, error codes mapped
to 400/404/409).

Placeholder — to be expanded in a later document.
