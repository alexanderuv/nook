# Kotlin / Gradle
.gradle/
.kotlin/
build/
!gradle/wrapper/gradle-wrapper.jar
*.class

# IntelliJ / IDEs
.idea/
*.iml
*.ipr
*.iws
.vscode/

# Agent tooling. Per-developer settings, named `.local` but not matched by the
# `*.local` rule below — which is why it is listed here rather than left to it.
.claude/settings.local.json

# Node / React (UI)
node_modules/
dist/
*.local

# Env & secrets
.env
.env.*
*.pem

# OS
.DS_Store

# Local dev artifact-store clones (LocalGitArtifactStore)
/.nook-local/
